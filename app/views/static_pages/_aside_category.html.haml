- @categories.each do |category|
  - cache category do
    = link_to category.title, show_category_path(category), :class=>"list-group-item-action list-group-item #{active_class(show_category_path(category))}"


  